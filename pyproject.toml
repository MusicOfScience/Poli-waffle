[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "poli-waffle"
version = "0.1.0"
description = "Browser-first Streamlit starter for Australian political intelligence work"
requires-python = ">=3.11"
